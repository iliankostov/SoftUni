#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv) {

    char *firstName = "Pesho";
    char *lastName = "Peshev";
    printf("%s %s", firstName, lastName);

    return (EXIT_SUCCESS);
}