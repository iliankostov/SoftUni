#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv) {
    
    char firstName[5] = {'P', 'e', 's', 'h', 'o'};
    char lastName[6] = {'P', 'e', 's', 'h', 'e', 'v'};
    
    printf("%s %s", firstName, lastName);

    return (EXIT_SUCCESS);
}